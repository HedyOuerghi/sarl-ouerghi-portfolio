---
interface Props {
  as?: "h1" | "h2" | "h3";
  class?: string;
}

const { as: Tag = "h2", class: className = "" } = Astro.props;
---

<Tag class:list={["section-title font-heading text-3xl font-semibold text-petrol sm:text-4xl", className]}>
  <slot />
</Tag>
